/**
 * Browser Supabase client — re-export for @/lib/supabase/client imports.
 * Session is stored in sb-*-auth-token cookies (chunked) for proxy/server reads.
 */
export { supabase } from "@/lib/supabaseClient";
